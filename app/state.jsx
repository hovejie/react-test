import React,{Component} from 'react';
import ReactDOM from 'react-dom';
class Starts extends Component{
    render(){
        return (
            <p>123</p>
        )
    }
}
/*导出模块，default只能导出一个模块不能导出多个模块
  导出多个模块用：export {模块一，模块二}
 */

export default Starts;
//export（导出）
//import（导入）